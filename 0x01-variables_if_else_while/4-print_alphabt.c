#include  <stdio.h>
/**
 * main entry point
 *
 * Description: this code that print exception q & e
 *
 * Results : 0  as value
 *
 */

int main(void)
{
	char i
	for (i = 'a'; i <= 'z'; i++)
		if (i == 'e' && i == 'q')
			printf("%c", "grep[eq]", i);
}
